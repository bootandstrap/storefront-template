{
    "name": "@bootandstrap/shared",
    "version": "0.2.0",
    "private": true,
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "type-check": "tsc --noEmit",
        "test:run": "vitest run",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "zod": "^4.3.6"
    },
    "devDependencies": {
        "@types/node": "^22.0.0",
        "typescript": "^5.9.0",
        "vitest": "^3.0.0"
    }
}